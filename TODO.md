T3:
- Maybe add `counter` condition? I know but that's really necessary? (will see if i can do when i have time, will be like `group`, `comparisonType` and `value`, and similar like `timer` condition)